# DZEVA Branding Rules

Public product branding is DZEVA and company branding is ECHUKU. User-facing copy must not expose provider companies or raw runtime model names. DZEVA-native model labels and English capability descriptions are the public contract.

Internal namespaces, package names, adapter keys, database columns, and provider identifiers remain unchanged when required for runtime compatibility.
