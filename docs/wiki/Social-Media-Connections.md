# Social Media Connections

Connected social accounts belong to one authenticated DZEVA user. A connection is publishable only when the provider is supported, authorization completed, and the token is unexpired or has no expiry. OAuth scopes and account identifiers must match the provider's posting mode.

Expired accounts should be reconnected. Never display or log access tokens.
